using UnityEngine;

public class SoundInfo
{
    public string id;
    public float volume;

    public SoundInfo (string _id, float _volume)
    {
        id = _id;
        volume = _volume;
    }
}
